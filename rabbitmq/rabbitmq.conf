disk_free_limit.absolute = 2147483648
log.file.level = info
heartbeat = 60

# RabbitMQ 4.x: celery/kombu (mingle, pidbox, rpc-backend reply queues)
# declare transient non-exclusive queues; permit them until clients are upgraded.
deprecated_features.permit.transient_nonexcl_queues = true
