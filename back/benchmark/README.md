# Miminet Benchmark

Инструменты для измерения производительности эмуляции сетей Miminet.

---

## 📖 Содержание

- [Как запустить](#как-запустить)
- [Формат результатов](#формат-результатов)
- [Описание этапов](#описание-этапов)
- [Интерпретация](#интерпретация)

---

## Как запустить

### Требования:
- Docker
- Привилегированный режим (для Mininet)
- ~30-60 минут для полного бенчмарка

> **⚠️ Важно для Arch Linux и других rolling-release дистрибутивов:**
> ```bash
> # Загрузите модуль openvswitch на хосте перед запуском
> sudo modprobe openvswitch
> ```

### Быстрый запуск:
```bash
cd back/benchmark

# Быстрый тест (~1 минута)
./run_quick_benchmark.sh quick --output-file test.txt

# Средний тест (~5 минут)
./run_quick_benchmark.sh medium --output-file test.txt

# Полный тест (~30 минут)
./run_quick_benchmark.sh full --output-file test.txt
```

### Полная версия:
```bash
# Конкретные сети
./run_benchmark_in_docker.sh \
  tests/test_json/router_network.json \
  tests/test_json/vlan_with_stp_network.json \
  --iterations 20 \
  --output-file my_benchmark.txt

# Все сети
./run_benchmark_in_docker.sh \
  tests/test_json/*_network.json \
  --iterations 20 \
  --output-file full.txt \
  --continue-on-error
```

### Параметры:
- `--iterations N` - количество повторений (по умолчанию: 5)
- `--output-file FILE` - имя файла результатов
- `--continue-on-error` - продолжать при ошибках

> **📁 Примечание**: Результаты сохраняются в `benchmark_output_*/` .

---

## Формат результатов

```
================================================================================
MIMINET EMULATION BENCHMARK REPORT
================================================================================
Iterations per network: 20
Networks tested: 18

--------------------------------------------------------------------------------
Network: vlan_with_stp_network
--------------------------------------------------------------------------------
Total time (mean): 14.265s ± 0.050s

Stage                 Mean(s)    Std(s)   Share(%)
--------------------------------------------------
animation_creation      0.001     0.000       0.01
job_execution           3.078     0.015      21.58
network_init            0.409     0.024       2.87
network_start           8.311     0.028      58.26 
network_stop            2.465     0.036      17.28
packet_grouping         0.000     0.000       0.00
topology_creation       0.000     0.000       0.00
```

После завершения создаются два файла:
- `<name>.txt` - человекочитаемый отчет
- `<name>.txt.json` - JSON для анализа

---

## Описание этапов

### 1. topology_creation
Создание объекта топологии из JSON. Типичное время: <0.01s

### 2. network_init
Инициализация Mininet (создание хостов, свичей, роутеров). Типичное время: 0.1-0.7s

### 3. network_start 
**Ключевой этап оптимизации.**
- Запуск узлов сети
- Настройка VLAN/VXLAN/VTEP
- Ожидание готовности (STP/RSTP convergence, DHCP)
- Начало захвата трафика

Типичное время:
- Non-STP сети: 3s
- RSTP сети: 5-11s
- STP сети: 8-40s

### 4. job_execution
Выполнение команд (ping, traceroute, DHCP). Типичное время: 0.1-13s

### 5. packet_grouping
Группировка захваченных пакетов. Типичное время: <0.01s

### 6. animation_creation
Создание данных для визуализации. Типичное время: <0.01s

### 7. network_stop
Остановка трафика, очистка VLAN bridges, остановка Mininet. Типичное время: 2-3s

---

## Интерпретация

### Share(%)
Процент времени этапа от общего времени эмуляции.

**Пример:**
```
network_start    8.311s    58.26%  # 58% времени на запуск сети
job_execution    3.078s    21.58%  # 22% на выполнение jobs
```

### Std (Standard Deviation)
Стабильность измерений:
- **< 0.05s** - отлично
- **0.05-0.1s** - хорошо
- **> 0.1s** - возможны флуктуации

