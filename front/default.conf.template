# server {
#     listen 80;
#     server_name ${QUIZ_DOMAIN};
#
#     location / {
#         include uwsgi_params;
#         uwsgi_pass quiz:80;
#     }
# }

server {
    listen 80;
#    server_name ${BASE_DOMAIN};

    location /ai/ {
        include uwsgi_params;
        uwsgi_pass miminet:80;
        uwsgi_read_timeout 300;
        uwsgi_send_timeout 300;
    }

    location / {
        include uwsgi_params;
        uwsgi_pass miminet:80;
    }
}
